# Website UI kit · home page

inov8ortho.com recreation — header through procedure grid

The Website UI kit · home page showcase page from the standalone version (`ui_kits/website/index.html`): plain HTML with the system’s classes — copy the markup.
