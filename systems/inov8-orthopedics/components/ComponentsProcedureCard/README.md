# Procedure tile

Whole card is the link

The Procedure tile showcase page from the standalone version (`preview/components-procedure-card.html`): plain HTML with the system’s classes — copy the markup.
