# CTA block · dark

Navy + gradient wash

The CTA block · dark showcase page from the standalone version (`preview/components-cta-block.html`): plain HTML with the system’s classes — copy the markup.
