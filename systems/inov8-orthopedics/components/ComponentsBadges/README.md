# Badges & chips

Pill chips, status dots, filters

The Badges & chips showcase page from the standalone version (`preview/components-badges.html`): plain HTML with the system’s classes — copy the markup.
